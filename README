Datastructs Package

This package provides Go implementations of:

  * BitVector
  * HashSet

How to install:

    $ git clone git@github.com:jbrukh/datastructs.git
    $ cd datastructs.git/src && ./build install

Thanks to go-gb (https://github.com/skelterjohn/go-gb/) for
providing multi-package build support.

Example:

    package main
    import (
        "container/bitvector"
        "fmt"
    )

    func main() {
        v := bitvector.New(10)
        v.Set(1,true)
        fmt.Println(v)
    }
